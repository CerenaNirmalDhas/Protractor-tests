# Node.js with Angular
# Build a Node.js project that uses Angular.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:

- task: Npm@1
  inputs:
      command: 'install'
      workingDir: 'ProtractorEndToEndTest-master/EndToEndTests/EndToEndTests'

- task: CmdLine@2
  inputs:
    script: 'node_modules/protractor/bin/webdriver-manager update'
    workingDirectory: 'ProtractorEndToEndTest-master/EndToEndTests/EndToEndTests'

- task: CmdLine@2
  inputs:
      script: 'node_modules/protractor/bin/protractor e2e/protractor.conf.js'
      workingDirectory: 'ProtractorEndToEndTest-master/EndToEndTests/EndToEndTests'


- task: PublishTestResults@2
  inputs:
    testResultsFormat: 'JUnit'
    testResultsFiles: 'xmlresults.xml'
    searchFolder: '$(System.DefaultWorkingDirectory)/ProtractorEndToEndTest-master/EndToEndTests/EndToEndTests/testResults/$(time)/'
